import 'package:combine_mobile/sections/responsive/grid-sample-page.dart';
import 'package:flutter/material.dart';

class GridSamplePageApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new MaterialApp(
      title: 'Grid App',
      home: GridSamplePage(),
    );
  }
}
